import {observer} from "mobx-react-lite";
import {Container} from "react-bootstrap";
import React from "react";

const Edit_Courses = observer(() =>  {
    return (
        <Container>
            Ураааа мы на странице редактирование отзывов
        </Container>
    )
});

export default Edit_Courses